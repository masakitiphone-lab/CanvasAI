# CanvasAI Development Log

This is the timestamped handoff log for major repo changes.

Use this file when a new AI or developer joins and needs to understand:

- what changed
- when it changed
- what the repository state became
- what should happen next

## 2026-04 Calendar

### 2026-04-02

- `19:16` Persistent prompt-composer and image-generation planning pass completed. Kept user prompt nodes in composer mode even after they have already sent once, so files, model selection, and resend controls remain available on the same node, and recorded the future image-generation direction in architecture memory: Vertex AI image-capable models, inline preview, larger preview on focus, and explicit download affordances.
  Repository state: prompt nodes now behave more like reusable chat composers rather than one-shot forms, and the repo memory now has a concrete direction for image-generation nodes instead of vague future hand-waving.
  Verification state: `npm run build` remains green after keeping user nodes in composer mode. The running app returns HTTP `200` at `http://127.0.0.1:3000`.
  Next recommended step: implement image-generation nodes using a dedicated Vertex AI route, return image metadata plus asset URLs, and add inline preview + download actions directly on the node.

- `19:10` AI-node size alignment and attachment-style pass completed. Fixed the mismatch between the React Flow node wrapper size and the AI card's internal minimum width by aligning the shared default AI size constants, and restyled attachments into cleaner rectangular chips plus square image thumbs with larger removable `×` controls.
  Repository state: AI response nodes should now stop visually overshooting their actual React Flow bounds, which means resize/selection overlays line up with the card again, and prompt attachments now read more like real product chips instead of tiny utility tags.
  Verification state: `npm run build` passes after the AI size-constant alignment and attachment-style rewrite. The running app returns HTTP `200` at `http://127.0.0.1:3000`.
  Next recommended step: verify three exact interactions together: select a long AI response node and confirm the visible card matches the actual node bounds, attach a PDF and remove it with the larger `×`, and attach an image to confirm the square thumb rendering feels closer to the intended reference.

- `19:04` Prompt-composer polish pass completed. Replaced the ugly native model dropdown with a custom pill-style model menu, forced the send action back to a clear black circular button, rebuilt attachment chips into cleaner removable pills with `×`, and re-centered the AI branch handle on the node's right edge so it no longer looks physically misaligned.
  Repository state: the prompt footer now reads like a deliberate product control bar instead of browser-default form controls, attachments can be removed directly from their chip UI, and the reply-branch handle now sits on the true right-edge center of the node instead of drifting off-axis.
  Verification state: `npm run build` passes after the model-menu rewrite, attachment-chip cleanup, send-button hardening, and handle alignment update. The running app returns HTTP `200` at `http://127.0.0.1:3000`.
  Next recommended step: verify four exact interactions together: open the model pill menu and switch options, attach then remove a file from a prompt chip, confirm the send button stays visibly black in all prompt states, and visually inspect the right branch handle against the node edge at multiple zoom levels.

- `18:56` Draft-prompt persistence pass completed. Changed prompt-node editing rules so unsent user prompts keep their composer footer visible even after selection is cleared, as long as that node has not produced an AI child yet.
  Repository state: a fresh or unsent prompt node now behaves like an active draft surface instead of collapsing into a plain card the moment the user clicks elsewhere; model selection, file attachment, and send controls remain available until the first AI response is generated from that prompt.
  Verification state: `npm run build` passes after the draft-prompt editing-state rewrite. The running app returns HTTP `200` at `http://127.0.0.1:3000`.
  Next recommended step: verify two exact interactions together: create a fresh prompt node and click elsewhere to confirm the composer controls remain visible, then generate an AI response from that node and confirm the prompt stops behaving like an always-open draft.

- `18:50` Prompt-toolbar and future-image-node prep pass completed. Rebuilt the editable prompt footer into a more product-like control bar with a black circular send button, a visible microphone placeholder, a Gemini model selector (`Fast` / `Faster` / `Smart` labels), and a `+` tool menu that now exposes future-facing entries such as `Create image`, `Deep research`, and `Web search`. Also increased edge contrast so node connections are easier to follow.
  Repository state: prompt nodes now feel more like an actual chat composer instead of loose utility buttons, the selected Gemini model is stored on the user node and used when generating the next AI response, the `+` menu now establishes the future extension point for image generation and richer node types, and graph edges are easier to see against the canvas background.
  Verification state: `npm run build` passes after the prompt-toolbar rewrite, node-level model selection wiring, tool-menu addition, and edge visibility increase. The running app returns HTTP `200` at `http://127.0.0.1:3000`.
  Next recommended step: verify five exact interactions together: open the `+` menu, switch the prompt model label, confirm the microphone button sits correctly as a placeholder, send from the black circular arrow button, and inspect whether the darker connection lines still feel clean at multiple zoom levels.

- `18:45` Canvas-navigation pass completed. Replaced the sidebar's root-chat list with a true canvas list, renamed the primary action to `New canvas`, wired canvas switching so each selection loads its own blank-or-saved workspace through the existing `/api/canvas` projectId path, realigned the AI branch handle to the node's right edge, and switched the canvas background from dots to a faint grid.
  Repository state: the left rail now represents canvases instead of root nodes, new canvases start empty by design, switching canvases swaps the persisted graph instead of merely focusing a root node, the branch handle sits properly on the right edge instead of drifting off alignment, and the workspace background now uses a subtle line grid instead of dotted paper.
  Verification state: `npm run build` passes after the canvas-switching state rewrite, sidebar relabeling, handle-position update, and grid-background change. The running app returns HTTP `200` at `http://127.0.0.1:3000`.
  Next recommended step: verify four exact interactions together: create `New canvas`, switch between two canvases and confirm one can stay blank while the other keeps its nodes, drag from the AI branch handle and confirm it starts on the node edge cleanly, and inspect the new grid background at multiple zoom levels.

- `18:21` Range-selection and Markdown-instruction pass completed. Changed drag-box selection so it only activates while `Ctrl/Cmd` is held, kept additive node selection on `Shift + click`, restored a proper text cursor inside node bodies and prompt editors, widened AI response cards again so roughly fifty characters fit across more comfortably, and added an explicit Gemini `system_instruction` that tells the model to answer in compact CommonMark/GFM-style Markdown instead of drifting into random formatting.
  Repository state: normal left-drag still pans the canvas, `Ctrl/Cmd + left-drag` now owns box selection without fighting ordinary pan, text inside nodes feels copyable instead of wearing the wrong cursor, AI responses have a more realistic default reading width, and Gemini is now explicitly guided toward clean Markdown output instead of relying on luck.
  Verification state: `npm run build` passes after the React Flow modifier-state wiring, cursor cleanup, AI width increase, and Gemini system-instruction update. The running app returns HTTP `200` at `http://127.0.0.1:3000`.
  Next recommended step: verify four exact interactions together: plain left-drag canvas pan, `Ctrl/Cmd + left-drag` drag-box selection, `Shift + click` additive selection, and one fresh AI response to confirm the wider card plus Markdown instruction produce a cleaner reading surface.

- `18:02` Selection-gesture and edit-dismissal pass completed. Changed additive node selection to `Shift + click`, enabled drag-box area selection only on `Ctrl/Cmd + left-drag`, restored plain left-drag for normal canvas panning outside that modifier, made node text explicitly selectable with the mouse inside prompt and AI bodies, and ensured an actively edited prompt node exits edit mode when the user clicks another node or the pane.
  Repository state: multi-select no longer depends on the wrong key combination, area selection now lives behind a deliberate modifier instead of competing with normal canvas movement, text inside nodes can be highlighted and copied like a real app, and prompt editing no longer lingers after the user moves focus elsewhere.
  Verification state: `npm run build` passes after the React Flow selection-key rewrite, text-selection overrides, and edit-dismissal wiring. The running app returns HTTP `200` at `http://127.0.0.1:3000`.
  Next recommended step: verify four exact interactions together: `Shift + click` additive node selection, `Ctrl/Cmd + left-drag` box selection, normal left-drag canvas panning without a selection box, and editing a prompt node then clicking elsewhere to confirm edit mode closes immediately.

- `17:47` Grok-style Markdown rendering pass completed. Kept the existing `react-markdown` + `remark-gfm` stack, but rebuilt the renderer styling so headings, blockquotes, code blocks, inline code, tables, links, images, lists, emphasis, and horizontal rules all render with a cleaner modern reading rhythm instead of the previous generic default output.
  Repository state: AI responses still use the same safe Markdown pipeline, but the visible output now looks much closer to a polished chat product: headings have hierarchy, code blocks read like real code blocks, tables have bounded containers, blockquotes are visually separated, images render inside rounded media frames, and inline formatting no longer looks like raw browser default junk.
  Verification state: `npm run build` passes after the Markdown renderer and CSS updates. The running app returns HTTP `200` at `http://127.0.0.1:3000`.
  Next recommended step: verify one AI response containing a heading, list, blockquote, table, link, inline code, fenced code block, and image so the Grok-style reading surface can be judged as a whole instead of item by item.

- `17:30` Focused-node camera zoom and sidebar pass completed. Removed the separate AI detail window behavior, switched AI title double-click to an in-place focus mode that enlarges the node and smoothly zooms the camera toward it, rebuilt the sidebar into a ChatGPT-like shell with `New chat`, `Search chats`, root-chat selection, and a bottom account block, and moved the AI branch handle away from the resize zone so branching no longer fights resizing on the node edge.
  Repository state: AI response detail viewing now stays inside the canvas instead of opening a detached reading surface, the sidebar now behaves like a real chat navigator instead of a placeholder slab, active chats can be selected from the left rail, and the right-side reply handle is no longer sitting on top of the resize interaction.
  Verification state: `npm run build` passes after the in-place focus camera rewrite, sidebar reconstruction, spacing reduction, and handle-hit-area fix. The running app returns HTTP `200` at `http://127.0.0.1:3000`.
  Next recommended step: verify four exact interactions together: double-click an AI node header and confirm the camera smoothly zooms in, click a chat item in the sidebar and confirm the canvas focuses it, drag from the AI reply handle without hitting resize first, and run `オートレイアウト` once to confirm the tighter spacing still reads cleanly.

- `16:54` AI node sizing and tighter branch-spacing pass completed. Increased the default AI card footprint and raised the auto-size cap, enabled resize hit zones even when a node is not selected, reduced horizontal and vertical branch spacing again, and aligned AI-originated reply insertion to the top edge of the source node instead of offsetting it downward.
  Repository state: AI response cards now start larger and stay more readable by default, node resizing no longer requires a prior click-selection step, newly created branches sit noticeably closer to their parent chain, and AI-originated replies now launch from the top line of the source node instead of drifting lower.
  Verification state: `npm run build` passes after the larger AI sizing defaults, always-on resize zones, tighter insertion spacing, and top-aligned AI reply placement. The running app still returns HTTP `200` at `http://127.0.0.1:3000`.
  Next recommended step: verify three exact interactions together: resize an unselected node directly from its edge, create a new AI reply branch and confirm top alignment, and compare branch spacing before and after `オートレイアウト`.

- `16:40` Canvas performance cleanup completed. Memoized the conversation node component with a custom equality check so unrelated nodes stop rerendering during drag operations, paused autosave while any node is actively dragging, enabled React Flow's visible-elements rendering optimization, and deleted stale `canvas-composer` CSS that no longer belongs to the actual UI.
  Repository state: node dragging should feel lighter because the canvas no longer needlessly repaints every card on every movement frame, save scheduling no longer churns while a drag is in progress, only visible elements are rendered when possible, and the stylesheet no longer carries dead composer-era baggage.
  Verification state: `npm run build` passes after the node memoization, drag-time autosave guard, visible-element rendering flag, and dead CSS removal. The running app still returns HTTP `200` at `http://127.0.0.1:3000`.
  Next recommended step: verify two exact interactions together on a busier canvas: drag one node through several branches and pan around while watching whether motion is materially smoother than before.

- `16:31` Auto-layout and node-menu correction pass completed. Fixed the dagre layout helper so it uses each node's real width and height instead of pretending every card is the same size, tightened layout spacing again, added node-level right-click actions for reply and delete, removed the header `×` control entirely, and introduced subtle user/AI color separation in the node chrome.
  Repository state: `オートレイアウト` no longer shoves mixed-size cards into a fake one-size-fits-all grid, node right-click now exposes actual workflow actions instead of only pane creation, prompt nodes no longer carry the redundant close control, and user versus AI nodes are easier to scan at a glance because their top chrome is no longer visually identical.
  Verification state: `npm run build` passes after the dagre size fix, node context-menu wiring, header-action cleanup, and user/AI chrome color split. The running app still returns HTTP `200` at `http://127.0.0.1:3000`.
  Next recommended step: verify four exact browser interactions together: run `オートレイアウト` on a mixed-size graph, right-click a user node and use `返信`, right-click any node and use `削除`, and visually confirm the user/AI color split reads clearly without turning the UI into rainbow trash.

- `16:20` Prompt-node action and AI sizing pass completed. Turned the header close button red so it is actually visible, restored an obvious in-node send button as a black circular action, added a `+` attachment button for file uploads directly from editable prompt nodes, and made AI node width scale up with content length while still respecting a capped auto-size.
  Repository state: editable prompt nodes now behave more like a real chat input surface with visible close, attach, and send affordances inside the node shell; file attachments can be added after node creation through the `+` control; and AI response cards no longer stay stuck at one narrow width regardless of content density.
  Verification state: `npm run build` passes after the prompt action rewrite, file-upload hook wiring, and content-aware AI width adjustment. The running app still returns HTTP `200` at `http://127.0.0.1:3000`.
  Next recommended step: verify three exact browser interactions together: click `+` to attach one image or PDF to a prompt node, confirm the black send button is visible while editing, and compare a short AI response node against a long one to confirm width growth stays within the intended cap.

- `16:12` Branch-placement and prompt-node chrome pass completed. Made AI-handle drag-out respect the release point instead of snapping far away, reduced default horizontal and vertical spacing for both insertion and auto-layout, moved the prompt close control into the node header, restored a clear bottom-right send button inside the prompt card, and expanded the canvas zoom range in both directions.
  Repository state: dragging out from an AI response now creates the next prompt node near the actual drop location instead of teleporting it elsewhere, auto-created AI responses sit materially closer to their parent prompt, prompt nodes now use header `×` plus bottom-right send affordance instead of the previous muddled footer controls, and the canvas can zoom farther in and farther out than before.
  Verification state: `npm run build` passes after the placement-rule rewrite, prompt-node chrome cleanup, spacing reduction, and zoom-range expansion. The running app still returns HTTP `200` at `http://127.0.0.1:3000`.
  Next recommended step: verify four exact browser interactions together: drag from an AI handle and release at an arbitrary point, send from a fresh prompt node, run `オートレイアウト` to confirm nodes are no longer excessively far apart, and test the wider zoom range at both extremes.

- `15:52` Custom resize-overlay pass completed. Removed the visible React Flow resize boxes, replaced them with wider invisible edge-and-corner hit zones on selected nodes, wired left/top/corner resizing so node position updates correctly instead of only stretching to the right or bottom, and kept node content inside fixed minimum shells with internal scroll once the text exceeds the available body area.
  Repository state: selected nodes no longer show cheap visible resize squares, resizing now uses a larger invisible hit area with cursor feedback around the node shell, left/top/corner drags update both size and position correctly, and node content remains inside a fixed minimum card instead of collapsing into the edges.
  Verification state: `npm run build` passes after removing `NodeResizer`, adding the custom resize overlay, and wiring size updates through canvas state.
  Next recommended step: verify three exact browser interactions together: edge resize is easier to grab than before, top/left resize no longer drifts incorrectly, and long prompt/response content stays scrollable inside the minimum card instead of blowing the node open.

- `13:23` React Flow interaction cleanup completed. Disabled drag-box selection entirely, kept additive multi-select on `Ctrl/Cmd + click` only, increased visible inner padding again so text no longer hugs the node frame, and restored plainly visible React Flow resize controls so node resizing is actually operable instead of cosmetically hidden.
  Repository state: empty-pane left drag is no longer competing with a selection rectangle, multi-select now follows the simpler `Ctrl/Cmd + click` rule, node content has materially larger visible padding, and resize controls are back to a usable React Flow-default presentation rather than a fake invisible setup that looked clever and worked badly.
  Verification state: `npm run build` passes after the selection-mode reset, padding increase, and resize-control visibility reset. The running app still returns HTTP `200` at `http://127.0.0.1:3000`.
  Next recommended step: visually verify one selected node for three things at once: content no longer touches the frame, no drag-box appears on plain left-drag, and resize controls are actually grabbable at the edges/corners.

- `13:14` Selection-gesture and prompt-node UI reset completed. Switched drag-box multi-selection from `Shift` back to `Ctrl/Cmd` so normal left-drag remains available for canvas movement, redesigned prompt nodes so editing mode is send-first instead of edit-first, removed the ugly text close button in favor of an icon-only dismiss action, restored an explicit send button for user nodes, and strengthened selected-node highlighting again so the active node is obvious.
  Repository state: normal left-drag no longer competes with drag-box selection unless `Ctrl/Cmd` is held, prompt nodes now look more like compact input surfaces instead of clumsy forms, editing mode shows close/send icon controls instead of a large text button, and selected nodes are visually louder than the surrounding graph.
  Verification state: `npm run build` passes after the selection-key reset, prompt-node redesign, and stronger selected-node treatment. The running app still returns HTTP `200` at `http://127.0.0.1:3000`.
  Next recommended step: verify three exact browser interactions: `Ctrl/Cmd` + left-drag box selection, normal left-drag canvas panning without accidental selection, and the empty prompt node showing a visible send control while editing.

- `13:07` React Flow baseline-controls pass completed. Increased default prompt/AI widths again to stop text from hugging node edges, strengthened selected-node highlighting with a darker border plus ring, restored React Flow's full edge-and-corner resizer behavior so diagonal corner resize is available, added a top-right `オートレイアウト` action using the dagre helper, enabled `Delete`/`Backspace` removal for selected nodes, and switched multi-selection semantics to `Shift` so drag-box selection and additive selection are available without inventing extra custom behavior.
  Repository state: the canvas now exposes a more complete React Flow baseline toolset instead of a half-custom interaction layer; selected nodes are visually obvious, resize handles work on edges and corners, auto-layout can be triggered from the canvas chrome, and selected nodes can be deleted with the keyboard. Node spacing still relies on the current dagre assumptions plus overlap-avoidance for insertions.
  Verification state: `npm run build` passes after the width increase, selection highlight pass, auto-layout button wiring, delete key enablement, and `Shift` multi-selection setup. The running app still returns HTTP `200` at `http://127.0.0.1:3000`.
  Next recommended step: verify four browser interactions together: diagonal corner resize, `Shift` drag-box multi-select, `Delete` removal of selected nodes, and the `オートレイアウト` button resolving any remaining overlap from older saved layouts.

- `12:58` Node spacing and regenerate simplification pass completed. Increased the default prompt and AI node footprint, added more internal padding so text no longer hugs the card edge, switched resizing back onto React Flow's full `NodeResizer` system so all four sides and corners participate with standard resize cursors, and removed the regenerate configuration modal in favor of direct same-model regenerate from the AI node itself.
  Repository state: nodes now have more breathing room by default, AI cards start noticeably wider than before, selected nodes can resize vertically and horizontally from edges and corners without showing visible resize dots, and clicking `再生成` now immediately reruns the current AI node with its existing model and attachments instead of opening a second settings surface.
  Verification state: `npm run build` passes after the node-width increase, padding pass, resizer reset, and regenerate-modal removal. The running app still returns HTTP `200` at `http://127.0.0.1:3000`.
  Next recommended step: verify three browser interactions together: the prompt card should no longer look cramped, corner resizing should now work instead of only horizontal resizing, and AI `再生成` should fire immediately without a modal.

- `12:44` Mind-map input flow and resize UX overhaul completed. Replaced the floating root/reply composer with direct empty prompt-node creation, so right-click `チャットを開始` now inserts an editable user node immediately and dragging out from an AI node inserts an editable reply node immediately. Reworked node resizing from visible dot handles to invisible edge hit-areas with standard resize cursors, increased node padding, kept AI detail view on title double-click, and added collision-avoidance for newly inserted root/reply nodes so they no longer stack on top of each other.
  Repository state: root creation and reply creation now happen as actual nodes on the canvas instead of an external composer surface; user nodes only call Gemini when their own `生成` button is pressed; AI nodes remain the branching source; selected nodes resize from the edges with Windows-like cursor feedback instead of visible resize dots; the canvas still pans with drag and zooms with the mouse wheel; and edges now render as more obviously curved `simplebezier` connections.
  Verification state: `npm run build` passes after the direct-node creation rewrite, resize-control swap, collision-avoidance pass, and curved-edge normalization.
  Next recommended step: verify three browser interactions together on the running app: right-click -> `チャットを開始` creates an empty editable node in-place, dragging from an AI node creates a reply node without overlap, and hovering near a selected node edge changes the cursor before resize.

- `12:19` AI node sizing pass completed. Increased the default AI card footprint, added content-aware minimum height growth for AI responses, preserved internal scroll for long bodies, and added a one-click expanded detail view so oversized responses can be read in a larger dedicated surface instead of being cramped inside the graph.
  Repository state: AI nodes now start larger than prompt nodes, grow somewhat with response length up to a cap, keep scrollable content inside the node for normal graph use, and can open into a large focused reading surface with one click.
  Verification state: `npm run build` passes after the larger AI-node defaults, content-aware sizing logic, and full-detail overlay view were added.
  Next recommended step: verify in the browser that medium-length AI replies no longer feel cramped and that very long replies are still usable through the new expanded reading view.

- `12:16` Core mind-map controls completed. Added React Flow `NodeResizer` handles so selected nodes can be resized from their edges, normalized existing saved edges to curved default paths instead of the old straight/smoothstep look, moved the visible branch handle from user nodes to AI nodes, and added Ctrl/Cmd-based multi-selection rules aligned more closely with Miro/XMind-style canvas behavior.
  Repository state: node cards can now be resized directly, old and new edges share the same curved style, AI nodes are the visible branching source for follow-up questions, and multi-select is designed around Ctrl/Cmd interaction instead of conflicting with the right-click menu.
  Verification state: `npm run build` passes after the resizer integration, edge normalization pass, AI-handle switch, and selection-key setup.
  Next recommended step: verify three exact browser interactions: resize a node from its edge, drag out from an AI-node handle to create a reply branch, and Ctrl/Cmd-drag across multiple nodes to confirm selection does not fight the context menu.

- `12:03` Mind-map interaction refinement completed. Fixed child placement so newly generated AI nodes no longer overlap their parent prompts, increased internal node padding so text no longer hugs card edges, removed the duplicate user edit button, switched edges to a smoother curved presentation, and added a visible right-side action handle so user nodes can create AI children by drag-out interaction instead of relying only on a footer button.
  Repository state: prompt nodes now have a cleaner Notion/ChatGPT-style card rhythm, AI children spawn to the right with real spacing, curved connections read more naturally, user editing is triggered from a single pencil action, and the right-side node handle now behaves more like a proper mind-map branch affordance.
  Verification state: `npm run build` passes after the placement fix, node spacing pass, curved-edge update, and drag-out branch creation wiring.
  Next recommended step: manually verify two specific interactions in the browser: dragging from the user-node handle to open space should create a right-side AI child, and repeated root prompt creation should keep each new prompt/AI pair from colliding with the previous pair.

- `11:55` Mind-map behavior reset completed. Rebuilt nodes into always-compact fixed cards with larger internal padding, switched body content to fixed-height internal scroll regions, disabled implicit expand-on-select behavior, changed user-node editing to an explicit pencil-button flow only, enabled wheel zoom on the canvas, strengthened the dotted background, and changed root/reply prompt submission so each new user node immediately spawns a connected AI response node on the right.
  Repository state: the canvas now behaves more like a real mind map and less like an expanding chat inspector; nodes stay visually stable, user text is not directly editable until the edit button is pressed, AI output lives in its own scrollable region, and prompt submission creates the expected right-side AI branch automatically.
  Verification state: `npm run build` passes after the node-behavior reset, scroll-region update, wheel-zoom change, and automatic AI-child generation wiring.
  Next recommended step: verify the actual browser interaction sequence for root creation and reply creation, because this pass changed both display rules and generation timing in one shot.

- `11:36` Composer minimal pass completed. Removed the extra helper copy and attachment badges from the on-canvas composer, reduced the action row to just `+`, close, and a visible send icon, and switched React Flow pane behavior to prioritize free mouse panning over drag-selection so the canvas behaves more like a mind map surface.
  Repository state: the root composer is now a much quieter floating textbox, file attachment remains available only behind the plus button, and the canvas should drag freely with the mouse instead of feeling like a selection tool first.
  Verification state: `npm run build` passes after the composer reduction and pane-pan behavior change.
  Next recommended step: verify in the browser that the send icon is visible at all times and that dragging the empty pane no longer creates a selection rectangle.

- `11:02` Canvas interaction cleanup completed. Removed the empty-state message overlay so the canvas is movable immediately, rebuilt the on-canvas composer into a cleaner textarea-based shadcn surface, removed visible URL input from node creation and regenerate flows, added explicit React Flow drag handles to node headers, and cleaned up Japanese copy in the workspace and login screen.
  Repository state: the app now opens directly onto a dotted movable canvas, right-click still opens a minimal `チャットを開始` menu, clicking it places a floating textbox at the click point, created nodes can be dragged from their header area, and the primary chat flow no longer exposes URL-entry UI.
  Verification state: `npm run build` passes after the React Flow drag-handle pass, composer rewrite, URL-input removal, and login copy cleanup.
  Next recommended step: manually verify three interactions together in the browser: right-click root creation, dragging the created node by its header, and submitting one prompt through the visible send flow.

- `10:33` Sidebar restore and context-menu flow restore completed. Brought the sidebar back in a lighter form, restored the right-click menu interaction, and kept root-node creation as an on-canvas floating composer that appears only after clicking `チャットを開始`.
  Repository state: the app is back to a canvas-plus-sidebar structure, the root creation flow is explicit again, and the composer still lives on the canvas instead of docking to the bottom.
  Verification state: `npm run build` passes after restoring the sidebar and right-click menu path.
  Next recommended step: visually verify root composer placement and reply composer placement in the browser, then continue trimming shell copy until it feels quiet enough.

- `10:15` Canvas-first minimal UI pass completed. Removed the heavy sidebar shell, replaced it with a tiny floating brand chip, changed root-node creation to open a floating composer directly at the canvas click point, and reduced the header and empty-state copy so the canvas itself is the main surface.
  Repository state: the workspace now behaves more like a node canvas tool than a dashboard, the creation box appears on-canvas instead of docked at the bottom, and the overall screen is materially quieter and closer to a ComfyUI/mind-map style working surface.
  Verification state: `npm run build` passes after the shell simplification, floating composer conversion, and chrome reduction.
  Next recommended step: visually verify the floating composer position for both root creation and AI reply branching, then trim any remaining empty-state or login copy that still feels verbose.

- `09:51` Composer simplification completed. Rebuilt the node creation panel into a lighter single-bar composer, added drag-and-drop support for image/PDF files, compacted file and URL handling into a smaller secondary row, and fixed the canvas right-click action menu so it anchors relative to the canvas wrapper instead of drifting to the wrong place.
  Repository state: node creation now feels closer to a chat composer than a bulky modal form, drag-and-drop is available for file attachments, and the canvas context menu should appear where the user actually clicked.
  Verification state: `npm run build` passes after the composer rewrite and context-menu positioning fix.
  Next recommended step: verify the refreshed composer in the browser with one dropped image and one pasted URL, then decide whether to keep the context menu or replace it with an even lighter inline affordance.

### 2026-04-01

- `22:43` First-run cleanup completed. Removed the canned starter conversation from code and persistence, reset `canvasai-mvp` back to an empty canvas, simplified the sidebar so it no longer pretends there are multiple fake projects, and rebuilt `/login` into a cleaner split-panel screen instead of a rough placeholder card.
  Repository state: the app now opens without preloaded conversation nodes, the workspace explains the empty state directly on canvas, the sidebar reflects a single current project instead of fake research/demo entries, and the login route looks like a deliberate product surface instead of a stub.
  Verification state: `npm run build` passes, `/api/canvas?projectId=canvasai-mvp` now returns `snapshot: null`, `http://127.0.0.1:3000` returns the refreshed workspace shell, and `http://127.0.0.1:3000/login` returns the updated login copy.
  Next recommended step: run one full UI flow with a real image and a real PDF from the empty canvas, then verify generation, regenerate, persistence, and multimodal attachment handling together.

- `22:36` Live Supabase backend activation completed. Filled the server-side Supabase secret into local env, created the `canvas-attachments` Storage bucket, verified direct table access against the hosted project, restarted the app with the new env, migrated the `canvasai-mvp` project snapshot into Supabase, and confirmed `/api/canvas?projectId=canvasai-mvp` now returns `source: "supabase"`.
  Repository state: the main project is no longer stuck on local fallback mode, live table persistence is active, the Storage bucket exists, Gemini is configured locally, and the app can now read and write the canvas through the hosted Supabase backend.
  Verification state: `npm run build` passes with `.env.local`, `http://127.0.0.1:3000` returns HTTP `200`, direct Supabase API checks succeeded, and `/api/canvas?projectId=canvasai-mvp` now resolves from `supabase`.
  Next recommended step: test an actual image upload and a real PDF upload through the UI, then verify that image uses inline multimodal input and PDF uses the Gemini Files API path during generation.

- `22:19` Latest Gemini model pass completed. Switched the default model to `gemini-3-flash-preview`, removed the now-dead plain Gemini 3 Pro preview option from the UI, moved PDF handling onto the Gemini Files API, created `.env.local` from the provided secrets where possible, and confirmed a live `ok: true` response from the local Gemini route.
  Repository state: the app now defaults to the current Gemini 3 Flash preview path, image attachments go as `inlineData`, PDF attachments go through Gemini file upload and cleanup, and local environment config exists for Gemini plus partial Supabase setup.
  Verification state: `npm run build` passes with `.env.local` loaded, `http://127.0.0.1:3000` returns HTTP `200`, and a direct POST to `/api/gemini/generate` returned success using `gemini-3-flash-preview`.
  Next recommended step: get the missing Supabase `service_role` key and actual Postgres password from the dashboard, then run the schema and verify real DB/storage persistence instead of fallback local mode.

- `18:01` Multimodal Gemini wiring and persistence backend completed. Added real image/PDF attachment injection into the Gemini `generateContent` request, added `/api/canvas` save/load routes, introduced a Supabase-backed persistence layer with local JSON fallback, added a Supabase Storage proxy route, and created a `supabase/schema.sql` bootstrap file for tables.
  Repository state: image and PDF attachments now go to Gemini as binary request parts from the server, URL attachments still travel as text references, node and edge state can now persist through Supabase Postgres when configured, binary attachments can upload to Supabase Storage, and local JSON remains as a fallback development mode instead of the only storage path.
  Verification state: `npm run build` passes, `/api/canvas?projectId=canvasai-mvp` now responds successfully, local save/load route verification returns `source: "local"` without Supabase configuration, and the app still serves successfully on `http://127.0.0.1:3000`.
  Next recommended step: provision the actual Supabase project and bucket, then verify end-to-end persistence plus multimodal Gemini responses against real image and PDF uploads.

- `17:46` AI regenerate flow completed. Added in-place regenerate support for AI nodes, a dedicated regenerate settings panel, per-node Gemini model selection (`Flash`, `Flash Lite`, `Pro`), and attachment carry-forward or replacement behavior for regenerate runs.
  Repository state: selected AI nodes now show a regenerate action alongside reply branching, regenerate reuses the parent lineage instead of the AI node's own content, node-level model metadata is stored in local React Flow state, and attachment updates during regenerate are persisted through the same local attachment APIs used by node creation.
  Verification state: `npm run build` passes after adding the regenerate panel, model-config types, and AI-node state updates.
  Next recommended step: either wire true multimodal Gemini request parts from attachment metadata, or stop pretending local JSON storage is enough and move nodes plus attachments into a real persistence layer.

- `17:32` Mind-map layout refinement completed. Applied dagre-backed left-to-right tree placement to the seeded workspace, tightened insert-time spacing around `rankSep: 160` and `nodeSep: 80`, strengthened edge contrast to the requested muted gray, emphasized selected nodes with a black `2px` border and `shadow-md`, and strengthened the active project card state in the sidebar.
  Repository state: the canvas now reads more like an actual mind map instead of a loose horizontal graph, the main chain is easier to follow left-to-right, sibling branches sit lower with cleaner spacing, top-of-canvas chrome is reduced to a single right-click hint, and the visible UI remains Japanese by default.
  Verification state: `npm run build` passes, a local dev server is running and responding with HTTP `200` at `http://localhost:3001`, and Playwright browser verification is currently blocked by a local MCP permission issue trying to write under `C:\Windows\System32`.
  Next recommended step: stop polishing broad UI traits blindly and decide the next product slice, most likely real persistence, regenerate UX, or true multimodal Gemini request wiring.

- `17:12` Japanese UI pass completed and browser-verified. Switched the visible workspace, login screen, composer modal, node actions, seed content, and user-facing error text to Japanese, introduced `@dagrejs/dagre` for insertion-time branch positioning, reduced visible status badges to the MVP-critical set, and tuned edge styling closer to the requested muted mind-map presentation.
  Repository state: user-facing UI is now Japanese by default, new branch insertions use `dagre` suggestions while existing nodes remain where they are, node width still stays fixed at `300px`, and the product surface is closer to the requested Notion/ChatGPT-style minimal white-and-black structure.
  Verification state: `npm run build` passes, browser inspection confirms `/` and `/login` now render Japanese text, selected nodes show Japanese labels and actions, and the lighter edge styling remains in place.
  Next recommended step: choose the next behavior slice deliberately instead of continuing broad polish passes.

- `16:57` Shell consistency pass completed and browser-verified. Restyled the node creation panel and `/login` route to match the calmer `shadcn/ui` card language, replaced the remaining plain form controls with `shadcn` `Input`, `Button`, and `Card` components, rounded and softened the modal composer, and brought the placeholder login experience into the same visual system as the main workspace.
  Repository state: the main workspace, node composer, and login placeholder now share the same restrained visual language; interactive controls are consistently `shadcn/ui`-based, the composer no longer uses the older black offset shadow treatment, and the app looks materially more cohesive without changing behavior.
  Verification state: `npm run build` passes, `/login` renders the updated card-based placeholder, and browser interaction confirms the node creation panel opens from canvas right-click with the updated `shadcn` styling.
  Next recommended step: stop chasing generic polish and decide whether the next slice is real persistence, regenerate UX, or multimodal Gemini request wiring.

- `16:53` UI cleanup pass completed and browser-verified. Rebuilt the shell and node presentation around `shadcn/ui` cards and buttons, removed the heavy black drop-shadow look, reduced exposed debug and metric noise from the canvas header, fixed all nodes to a 300px width, softened the edge styling, switched the background grid to a lighter dot field, and pushed the canvas toward a cleaner mind-map product surface instead of an internal-tool debug screen.
  Repository state: the app still uses the same React Flow interaction model and Step 8 attachment flow, but the visible UI is now quieter and more product-like; the left sidebar is cleaner, the canvas header only shows action guidance and reset instead of raw counters, node badges are trimmed down to root/time/status, and the direct-lineage debug panel is no longer exposed in the main UI.
  Verification state: `npm run build` passes, browser inspection confirms every visible node now renders at `300px` width, the top-level debug counters are gone, and the canvas surface shows the simplified mind-map presentation with lighter edges and quieter chrome.
  Next recommended step: stop polishing blindly and decide the next product behavior slice, because the current bottleneck is no longer obvious shell ugliness.

- `16:38` Step 8 completed and browser-verified. Added creation-time attachment support for image, PDF, and URL inputs; introduced a node creation panel for root and reply user nodes; added attachment upload and URL registration API routes; stored attachment metadata in `data/attachment-metadata.json`; stored uploaded binary files in `public/storage/attachments`; rendered attachment cards inside expanded nodes; and extended the lineage payload so Gemini requests now carry attachment metadata hooks for future multimodal prompting.
  Repository state: the canvas still behaves like the same simple React Flow mind map, but new user nodes can now be created with attachments, expanded nodes display those attachments as simple cards, direct-lineage debug now shows attachment counts, Gemini still uses `generateContent` with a hardcoded `gemini-2.5-flash` model, the attachment persistence layer is intentionally a local JSON-and-filesystem MVP rather than a real production database, `/login` is still fake, and regenerate plus true multimodal Gemini requests are still not implemented.
  Verification state: browser flow created a new root node with a URL attachment card and another new root node with an uploaded PNG attachment card; direct API checks confirmed URL registration succeeds, invalid file types are rejected, valid image upload succeeds, `data/attachment-metadata.json` now exists, and uploaded files now exist under `public/storage/attachments`.
  Next recommended step: stop adding surface area and decide the next real product slice, because the originally planned MVP phases are now covered through Step 8.

- `15:26` Step 7 completed and browser-verified. Installed `react-markdown` and `remark-gfm`, added a dedicated Markdown renderer component, switched expanded AI nodes from plain text to safe Markdown rendering, updated seeded AI content so lists, tables, blockquotes, code blocks, links, and inline code are visible immediately, and moved the workspace status messaging from Step 6 to Step 7.
  Repository state: AI nodes now render Markdown with GFM support inside the existing capped scroll container, direct-lineage Gemini requests remain unchanged, sibling exclusion debug remains visible, the model is still hardcoded to `gemini-2.5-flash`, `/login` is still fake, there is still no attachment handling or storage layer, and successful live Gemini rendering still depends on a real `GEMINI_API_KEY`.
  Next recommended step: Step 8 only. Add MVP attachment support for image, PDF, and URL inputs at node creation time.

- `15:18` Step 6 completed and browser-verified. Added a lineage builder utility, replaced the naive Gemini request payload with explicit `{ targetNodeId, lineage, model }`, updated the API route to build prompts from that lineage only, seeded a sibling branch specifically for exclusion testing, and added a debug panel that shows the exact direct lineage sent for the selected node.
  Repository state: Gemini generation now uses the selected node's direct ancestor chain only, sibling branches are excluded by construction and visibly absent from the debug panel and request payload, the model remains hardcoded to `gemini-2.5-flash`, missing API keys still fail visibly into `error` nodes, `/login` is still fake, and there is still no attachment handling or storage layer.
  Next recommended step: Step 7 only. Render AI responses as Markdown using `react-markdown` and `remark-gfm`.

- `15:13` Step 5 completed and browser-verified against the failure path. Added a local `/api/gemini/generate` route, documented `GEMINI_API_KEY` in `.env.example`, added a user-node `Generate AI Reply` action, created AI child nodes immediately in `generating` state, and updated them to either filled `idle` responses or `error` nodes when the Gemini request fails.
  Repository state: the canvas still behaves like a simple React Flow mind map, but user nodes can now kick off minimal Gemini generation; the current request payload is intentionally naive and only sends the selected user node text, the model is hardcoded to `gemini-2.5-flash`, missing API keys fail deterministically into visible `error` nodes, `/login` is still fake, and there is still no lineage request builder, no attachments, and no storage layer.
  Next recommended step: Step 6 only. Replace the naive generation payload with explicit direct-lineage context building and prove sibling branches are excluded.

- `14:55` Step 4 completed and browser-verified. Kept React Flow as the only canvas library, shifted the presentation toward a plain mind-map look, added visible non-interactive parent-child edges, wired AI-node reply branching into child user nodes, inserted new children to the right with simple sibling-based vertical staggering, marked dragged nodes as pinned, and added viewport follow for off-screen insertions.
  Repository state: the main canvas now behaves like a basic node-based mind map with visible lineage links, seeded branches, AI reply branching, local-only insert layout, local-only pinning after drag, and simple smoothstep edges; the sidebar still remains mock data, `/login` is still fake, and there is still no backend, no Gemini, no lineage request builder, and no attachments.
  Next recommended step: Step 5 only. Connect Gemini `generateContent` in the simplest possible way by creating an empty AI node immediately, marking it `generating`, and resolving it to success or error without adding lineage logic yet.

- `14:46` Step 3 completed and browser-verified. Installed Tailwind CSS v4 and `shadcn/ui`, added reusable UI primitives, introduced typed node records, rebuilt the canvas nodes as `shadcn`-styled user and AI cards, enabled compact-by-default rendering with expand-on-selection behavior, made expanded nodes scroll internally, and enabled direct editing for user nodes while keeping AI nodes read-only.
  Repository state: the main canvas now opens with seeded local mock nodes that demonstrate both node kinds and all MVP status badges, right-click `New Chat` still creates a root user node, user node content edits stay local in React Flow state, the sidebar still remains mock data, `/login` is still fake, and there is still no backend, no Gemini, no lineage logic, and no attachments.
  Next recommended step: Step 4 only. Add parent-child reply behavior, insertion-time layout rules, visible edges, and viewport follow behavior without touching Gemini yet.

- `14:29` Step 2 completed and browser-verified. Installed `@xyflow/react`, replaced the fake canvas with a real React Flow canvas, enabled pan and zoom, added an empty-pane right-click context menu, implemented `New Chat` to create a root user node in local state at the clicked flow position, and fixed the container sizing so React Flow no longer renders with zero-height warnings.
  Repository state: `src/app/(workspace)/page.tsx` now renders a client-side flow canvas, `src/components/flow-canvas.tsx` owns the Step 2 local graph behavior, right-click on empty canvas opens the menu, selecting `New Chat` creates a draggable root node, the sidebar still remains placeholder-only, `/login` is still fake, there is still no backend, no Gemini, no attachments, and no real persistence.
  Next recommended step: Step 3 only. Implement node UI states, compact/expanded behavior, and editable user nodes without touching Gemini or uploads.

- `time-not-recorded` Step 1 completed earlier on the same day. Initialized Next.js App Router, created the workspace shell route, created the `/login` placeholder route, and established the black-and-white desktop-first shell UI.
  Repository state: shell routing and placeholder surfaces existed, but the canvas was still fake before the Step 2 upgrade.
  Next recommended step at that time was: install React Flow and replace the fake canvas with a real local-only graph surface.

### 2026-04-02

- `19:31` Gemini image-generation implementation completed. Added a dedicated `/api/gemini/generate-image` route using the official Gemini image-generation path, persisted generated image binaries through the existing attachment storage layer, introduced an explicit `image` node kind, wired the prompt-node `Create image` action to generate connected image result nodes, added in-node preview and download UI, and enabled image-node regenerate.
  Repository state: prompt nodes can now trigger both text replies and image generation without leaving the canvas, generated images are saved into the same attachment persistence system as uploaded files, image result nodes render large previews plus download actions, image nodes can branch follow-up prompts like AI response nodes, and the implementation now uses Gemini API image generation directly instead of the earlier placeholder plan.
  Verification state: `npm run build` passes, `http://127.0.0.1:3000` returns HTTP `200`, and a live POST to `/api/gemini/generate-image` returned `ok: true` with a stored JPEG attachment from `gemini-3.1-flash-image-preview`.
  Next recommended step: implement richer image-node interactions, specifically multi-image variants, larger in-canvas preview behavior, and image-specific context actions from the node menu.

- `22:16` Post-image-generation polish completed. Increased the default AI response footprint again, re-placed generated AI and image nodes after their final content size becomes known so they stop colliding with nearby blocks, and auto-focused the canvas onto newly generated image nodes so the image flow is actually visible instead of looking broken.
  Repository state: AI response cards now start larger, generated nodes are repositioned against real occupied space after content or image attachments arrive, and image generation is no longer easy to mistake for a no-op just because the result appeared off-screen or under another card.
  Verification state: `npm run build` passes, and a second live POST to `/api/gemini/generate-image` again returned `ok: true` with a stored JPEG attachment.
  Next recommended step: add image-node-specific context menu actions and multi-variant image generation so the image workflow stops at “usable” instead of “bare minimum.”

- `23:11` Prompt image-mode UX pass completed. Stopped auto camera movement for image generation, changed `Create image` into an explicit prompt-side mode switch instead of immediate generation, showed a visible `Image` mode chip inside the prompt composer, thickened edge rendering again, and added a pulsing animation to nodes while they are generating.
  Repository state: prompt nodes now make image mode obvious before send, the same black send button dispatches either text or image generation depending on the selected mode, canvas edges are easier to read at a glance, and generating nodes are visually alive instead of looking frozen.
  Verification state: `npm run build` passes.
  Next recommended step: make image mode persist cleanly across reloads in the hosted canvas backend and add a visible way to switch back to text mode from the same composer without relying only on the model pill.

- `00:03` File-node input workflow completed. Canvas now accepts direct drag-and-drop of images and PDFs onto empty space, creates standalone `file` nodes from dropped or menu-added files, exposes file-node output handles for prompt connections, and allows prompt generation to include connected file/image node context in addition to the direct lineage chain.
  Repository state: the canvas supports file-first workflows instead of forcing every asset through a prompt node, prompt nodes can receive multiple upstream file/image inputs via edges, the pane context menu includes a file-add path, and file nodes can branch into prompt nodes the same way AI/image result nodes already could.
  Verification state: `npm run build` passes after adding the `file` node kind, drop handlers, explicit edge creation, and prompt-input context enrichment.
  Next recommended step: clean up the remaining pane-menu label corruption, then add visible incoming-input chips on prompt nodes so connected file context is obvious before generation.

- `00:13` Prompt-node connector cleanup completed. Removed the useless prompt edit button from the header and made the prompt node's left-side target handle visible all the time so file/image nodes can clearly connect into it.
  Repository state: prompt nodes now read like proper input nodes instead of mixed editor cards, and the left input affordance is finally obvious instead of hidden behind invisible React Flow defaults.
  Verification state: `npm run build` passes.
  Next recommended step: show connected upstream files as visible chips inside prompt nodes so users can tell what context is attached before they send.

- `00:26` Clipboard and note-node groundwork completed. Added a `note` node kind, allowed memo-node creation from the canvas context menu, wired canvas paste so clipboard image files become standalone file nodes when focus is not inside a text field, and kept normal text paste behavior intact for prompt and note textareas.
  Repository state: the canvas can now act like a real scratchpad instead of only a prompt launcher, with memo nodes for freeform text and pasted/dropped files as first-class graph inputs.
  Verification state: `npm run build` passes.
  Next recommended step: expose connected upstream file/note/image chips on prompt nodes and finish cleaning the remaining pane-menu label corruption so the new flows are actually legible.

- `11:21` File-node loading UX and note editing repairs completed. File drops now create immediate placeholder file nodes in a generating state, show an inline progress bar while uploads are still reading, avoid duplicate filename rendering by using a generic file title plus a body card, and keep failed uploads visible instead of disappearing. Note nodes now accept text input again because note content updates are wired through the same node-content update path as prompt nodes, and the pane context menu labels were normalized.
  Repository state: dropping or adding a file gives instant visual feedback, file nodes no longer wait invisibly for upload completion, uploaded files settle into a larger dedicated card size, and note blocks behave like editable memo nodes instead of dead cards.
  Verification state: `npm run build` passes on Next.js `16.2.2`.
  Next recommended step: surface connected upstream file/image/note inputs as visible chips inside prompt nodes so users can see exactly what context will be sent before generating.

- `11:51` Gemini model selection and prompt-input context wiring completed. Replaced vague `Fast/Faster/Smart` labels with explicit Gemini model IDs in the prompt footer, expanded the selectable text-model list to current Gemini 3.1 and 2.5 variants, broadened prompt request building so all connected upstream nodes (including memo nodes) are included as explicit prompt context, and fixed the remaining node-menu labels.
  Repository state: prompt nodes now keep their chosen Gemini model by exact model name, the generate route accepts user/AI/file/image/note lineage entries, and connected memo nodes are no longer ignored when a prompt node sends a request.
  Verification state: `npm run build` passes on Next.js `16.2.2`.
  Next recommended step: show connected upstream note/file/image nodes as visible chips inside the prompt node so the user can inspect attached graph context before sending.

- `12:06` Node chrome compression and token display completed. Shortened the header height across node types, moved timestamps into the footer, added small token-count labels for generated nodes based on Gemini usage metadata, increased inner textarea/content padding while reducing wasted shell spacing, and corrected file-card sizing so file content sits inside the node instead of spilling out of it.
  Repository state: generated text and image nodes can now show their token usage, file nodes no longer visually overflow their parent block, and prompt/note nodes devote more area to actual input instead of bloated header chrome.
  Verification state: `npm run build` passes on Next.js `16.2.2`.
  Next recommended step: show connected upstream note/file/image nodes as visible chips inside the prompt node so attached graph context becomes inspectable before sending.

- `12:51` Markdown typography pass completed. Increased heading scale and spacing in the Markdown renderer, tightened paragraph/list rhythm, improved blockquote and code block emphasis, and aligned the renderer toward a structured Grok-style reading surface while keeping the same `react-markdown + remark-gfm` stack.
  Repository state: AI response nodes now render headings with meaningful size separation, lists and tables breathe more cleanly, and code/quote sections stand out instead of collapsing into the same text density as body paragraphs.
  Verification state: `npm run build` passes on Next.js `16.2.2`.
  Next recommended step: add explicit visible chips for connected note/file/image inputs inside prompt nodes so the rendered output and the sent context are both inspectable.

- `13:03` Markdown renderer foundation swapped from custom CSS-first styling to a Tailwind typography base. Installed `@tailwindcss/typography`, `remark-math`, `rehype-katex`, and `katex`, enabled the typography plugin in global CSS, and moved the Markdown surface onto `prose`-driven styling with explicit overrides for headings, lists, tables, code blocks, links, and images.
  Repository state: Markdown rendering now has a more maintainable base, tables still come through `remark-gfm`, LaTeX-style math is now supported through KaTeX, and future typography tweaks can happen mostly through `prose-*` classes instead of brittle hand-written element-by-element CSS.
  Verification state: `npm run build` passes on Next.js `16.2.2`.
  Next recommended step: add explicit visible chips for connected note/file/image inputs inside prompt nodes so the rendered output and the sent context are both inspectable.

- `13:37` Markdown renderer redesign completed. Rebuilt the AI Markdown surface around a `shadcn-chatbot-kit` / `Prompt Kit` style class structure, pushed fenced code blocks into `not-prose` wrappers so prose stops mangling them, and strengthened the H1-H4 hierarchy toward `shadcn-typography` sizing instead of the flatter previous pass.
  Repository state: the Markdown stack is still `react-markdown + remark-gfm + remark-math + rehype-katex`, but the visual layer now clearly separates narrative prose from isolated code surfaces, headings carry more hierarchy, and the remaining global Markdown CSS is now supporting the renderer instead of fighting it.
  Verification state: `npm run build` passes on Next.js `16.2.2`.
  Next recommended step: widen the AI-node body container and surface connected upstream file/image/note chips inside prompt nodes, because typography is cleaner now but the surrounding node chrome still needs work.

- `13:54` Markdown typography values were tightened to explicit reading metrics. Body copy now renders at `15px` with `1.75` line-height, headings follow a tighter `24/20/18px` hierarchy with stronger top spacing, lists use shallower indentation and cleaner item rhythm, inline code follows the lighter `#f5f5f5` chip style, fenced code blocks now use a stable `#0f172a` / `#e5e7eb` treatment, and blockquotes were flattened toward a Notion-like left-rule presentation instead of the heavier card treatment.
  Repository state: the renderer now follows explicit typography numbers instead of vague “looks better” tuning, which makes future refinement less hand-wavy and more inspectable.
  Verification state: `npm run build` passes on Next.js `16.2.2`.
  Next recommended step: widen the AI-node content area and reduce the surrounding node chrome density, because the markdown itself is less wrong now but the node container is still constraining it.

- `13:59` Markdown cleanup pass completed. Increased table cell padding so text stops touching grid lines, added a little more left padding to fenced code blocks so the first column no longer feels cropped, flattened blockquote inner spacing so the quote border and text stop fighting each other, and normalized some global Markdown helpers so tables and code surfaces behave more predictably.
  Repository state: the Markdown renderer now reads cleaner in the exact weak spots that were still making it look cheap: tables, blockquotes, and code blocks.
  Verification state: `npm run build` passes on Next.js `16.2.2`.
  Next recommended step: widen the AI-node body container and lower the surrounding shell density, because the text system is cleaner now but the card around it is still doing some damage.

- `15:04` AI-reading-surface pass completed. Increased the default AI node bounds and minimum width, added extra wrapper padding around table and code-block surfaces so rounded corners stop visually colliding with text, and strengthened the Gemini Markdown system instruction to prefer clearer section spacing, concise tables, and only sparse tasteful emoji when they genuinely help scanability.
  Repository state: AI response cards now start wider, markdown tables and fenced code blocks have actual breathing room inside their rounded wrappers, and Gemini has clearer formatting guidance instead of freewheeling into cramped dense output.
  Verification state: `npm run build` passes on Next.js `16.2.2`.
  Next recommended step: reduce shell chrome density around the AI node body and verify real long-form responses, because the markdown surface is cleaner now but the outer card can still feel tight.

- `15:08` Markdown rhythm and AI-container pass completed. Increased the AI-node content padding, constrained markdown line length to a saner readable width, added dedicated spacing for display math, further strengthened table/code wrapper breathing room, and changed the Gemini formatting prompt so technical answers default away from emoji instead of accidentally looking childish.
  Repository state: long AI answers now read less like a stretched raw document dump and more like a deliberately typeset note inside the node, while tables, code blocks, and LaTeX displays get visibly more room to breathe.
  Verification state: `npm run build` passes on Next.js `16.2.2`.
  Next recommended step: reduce the remaining node-shell chrome density, because the markdown interior is now cleaner than the card around it.

- `15:15` Markdown/table polish pass completed. Converted table rendering away from the tighter collapse-first setup into a roomier wrapper with explicit inner border and larger first/last cell padding, widened the AI-node content padding again, narrowed the readable line length slightly, and enlarged display-math presentation so fractions and standalone equations stop collapsing into the surrounding paragraph flow.
  Repository state: the most obvious markdown cheapness is now reduced in tables, display math, and AI content spacing; the renderer behaves more like a deliberate reading surface and less like raw HTML inside a rounded card.
  Verification state: `npm run build` passes on Next.js `16.2.2`.
  Next recommended step: trim the remaining node-shell chrome density and re-check real Gemini outputs, because typography is cleaner now but live content still needs a sanity pass.

- `15:19` Markdown vertical-rhythm cleanup completed. Strengthened spacing differences between H1-H6 levels, turned blockquotes into actual tinted quote blocks instead of barely-visible one-line left borders, increased code-block outer margin so it no longer visually sticks to the next section divider, and added global sibling-spacing rules so headings after code/quote/table blocks stop collapsing into the previous block.
  Repository state: markdown block elements now have clearer layer separation and section rhythm, which means quotes, code blocks, and lower-level headings no longer read like one unbroken slab.
  Verification state: `npm run build` passes on Next.js `16.2.2`.
  Next recommended step: reduce node-shell chrome density and validate the renderer against real Gemini output, because the typography scaffold is finally cleaner than the outer card chrome.

- `15:23` Reference-image alignment pass completed. Rebuilt fenced code blocks into a ChatGPT-style surface with a light header, language label, and copy action; made tables compact bordered widgets instead of stretched full-width slabs; and tightened quote/list/heading styling toward the provided reference so markdown blocks now read more like product UI and less like raw prose with random borders.
  Repository state: the markdown renderer now has explicit product-style components for code and tables, rather than relying on prose-only styling, and the overall layout is materially closer to the supplied target image.
  Verification state: `npm run build` passes on Next.js `16.2.2`.
  Next recommended step: reduce the remaining node-shell chrome density and inspect real Gemini-generated markdown, because the renderer is now closer to the target reference than the outer card styling is.

- `15:31` Markdown reference pass completed. Restored real bullet and numbered list markers by overriding the global list reset inside markdown surfaces, switched fenced code blocks onto a dedicated syntax-highlighting library path (`react-syntax-highlighter`) instead of hand-tinted monospace text, and adjusted compact table rendering so tables behave like smaller bordered widgets instead of a stretched left-anchored slab.
  Repository state: markdown lists now show actual visible markers, code blocks are rendered through a purpose-built highlighting component with a utility header, and the renderer is less dependent on brittle hand-colored code styles.
  Verification state: `npm run build` passes on Next.js `16.2.2` after adding `react-syntax-highlighter` and `@types/react-syntax-highlighter`.
  Next recommended step: verify the live renderer against real Gemini output and then slim down the surrounding AI node chrome, because the markdown widget layer is finally ahead of the outer card styling.

- `15:42` Node-chrome and markdown-surface refinement completed. Slimmed the AI-node header/body/footer spacing, reduced chrome typography density, centered code/table widgets inside the reading column, fixed the code-widget copy label mojibake, and moved the content container closer to a clean white document panel instead of the earlier tinted slab.
  Repository state: the markdown renderer and the node shell are now less at odds with each other; code, table, and quote blocks sit more naturally inside a lighter reading surface, and the outer card chrome no longer overpowers the content as much.
  Verification state: `npm run build` passes on Next.js `16.2.2`.
  Next recommended step: compare one real Gemini response with nested lists, tables, code, and quotes against the target reference and do only the last mile tweaks instead of another broad rewrite.
- `2026-04-03 15:59 +08:00` Markdown widget and edge-interaction pass completed. Rebuilt the renderer so code blocks use roomier utility headers and inner padding, compacted tables into padded bordered widgets with larger first/last-cell spacing, strengthened quote/list spacing, raised the default AI node size substantially, slimmed node-shell chrome again, and turned edges into selectable/reconnectable React Flow elements with visible selected-state highlighting.
  Repository state: markdown blocks now behave more like product widgets than raw prose, AI response cards start taller and wider, and edges can finally be selected, deleted, and reattached instead of being inert decoration.
  Verification state: `npm run build` passes on Next.js `16.2.2`.
  Next recommended step: sanity-check one real Gemini response containing nested lists, a table, a quote, and a code block, then do only the remaining small spacing polish instead of another broad rewrite.
- `2026-04-03 16:11 +08:00` Prompt-creation anchor and markdown-rhythm pass completed. Dragging out from an AI/image/file node now creates the new prompt with its left-edge center aligned to the drop point instead of pinning the node's top-left corner there, newly created prompt/note textareas autofocus immediately for typing, and markdown list/quote/table transitions got another spacing pass so post-list paragraphs and following blocks stop collapsing together.
  Repository state: branch-created prompts now feel intentional instead of awkwardly offset, typing can start immediately after drag-creation, and markdown rhythm after lists/quotes/tables is cleaner without introducing another heavy library.
  Verification state: `npm run build` passes on Next.js `16.2.2`.
  Next recommended step: test one real Gemini response plus a drag-created prompt flow in the browser, then only decide whether markdown tables still need a specialized renderer; for static chat tables, the current custom widget is still a better tradeoff than dragging in a full table package.
- `2026-04-03 16:22 +08:00` Markdown list/hr rhythm pass completed. Replaced the earlier narrow post-list patch with global block-spacing rules so unordered/ordered lists now get their own top and bottom breathing room everywhere, and added explicit vertical spacing around horizontal rules so dividers stop collapsing into adjacent text.
  Repository state: markdown list sections and dividers now follow a more stable global rhythm instead of relying on brittle one-off adjacency hacks.
  Verification state: `npm run build` passes on Next.js `16.2.2`.
  Next recommended step: compare the new list/divider spacing against a live Gemini response, then continue table tightening only if the card edges still visually crowd the first column.
- `2026-04-03 16:30 +08:00` Markdown surface tightening pass completed. Increased the outer reading-column padding, widened table and code widget inner spacing again, strengthened quote styling so it reads as a distinct block instead of a plain line of text, and added Gemini prompt guidance to stop escaping normal emphasis markers or wrapping ordinary quoted Japanese text in code formatting.
  Repository state: code blocks, tables, and quotes now have more breathing room, first/last table columns no longer crowd the edges as aggressively, and Gemini is explicitly instructed to emit real emphasis markdown instead of literal escaped marker noise.
  Verification state: `npm run build` passes on Next.js `16.2.2`.
  Next recommended step: validate one real Gemini response that contains emphasis around Japanese quotes plus a table and blockquote, then only do the final visual polish that still survives that real output.
- `2026-04-03 16:37 +08:00` Persistence and prompt-width pass completed. Local canvas snapshots now persist node `style` alongside position/data so resized nodes reopen in the same shape on this machine, prompt nodes now default to a wider horizontal footprint, markdown tables switched away from the old `min-w-full` stretch behavior toward a compact width-based layout, and quote blocks were simplified away from the earlier pill-like shape.
  Repository state: reopening a canvas now preserves node size as well as coordinates through the local snapshot layer, new prompt cards start wider by default, and markdown tables/quotes are moving away from the cheap stretched look.
  Verification state: `npm run build` passes on Next.js `16.2.2`.
  Next recommended step: open one real canvas, resize a few nodes, reload, and verify the saved shape/position state; then keep tuning the markdown table/code spacing from live screenshots instead of guessing.
- `2026-04-03 16:45 +08:00` Markdown block-spacing normalization pass completed. Unified top/bottom rhythm for paragraphs, lists, quotes, dividers, tables, and code widgets; widened code-widget header/body padding again; tightened table layout away from the earlier awkward stretched/slab behavior; and restored quote-block inner padding so quotes stop reading like plain text glued to a border.
  Repository state: markdown blocks now share a more consistent vertical rhythm, code/table widgets have more believable interior breathing room, and tables no longer rely on the previous uneven width behavior that made columns look cramped and amateurish.
  Verification state: `npm run build` passes on Next.js `16.2.2`.
  Next recommended step: compare another live screenshot and only tune the remaining per-widget details (if any) instead of changing global spacing again.
